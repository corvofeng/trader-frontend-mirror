// Re-export from shared types for backward compatibility
export type { CurrencyConfig, RegionalColorConfig } from '../shared/types';
export { regionalColorConfigs } from '../shared/constants';
export { formatCurrency } from '../shared/utils/format';